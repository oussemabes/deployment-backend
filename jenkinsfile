pipeline {
    agent any

    tools {
        nodejs "node19"
    }

    stages {
        
        stage('Install Dependencies') {
            steps {
                sh 'npm install'
            }
        }

        stage('Docker deploy'){
            steps {
               
                sh "docker-compose up --build"
            }
        }
}
}
